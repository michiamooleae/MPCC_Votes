#include "uncroppedimagesfinder.h"

UncroppedImagesFinder::UncroppedImagesFinder()
{
    // 1. Get name of the country/issuer which has to be investigated
    // 2. Download the list of the countries webpage
    // 3. Extract the URL to the given country
    // 4. Download the country webpage
    // 5. Extract the number of the pages
    // 6. If more then one page, repeat 7 for each page
    // 7. Get list of entries, append to main list of entries
    // 8. For each entry, get images (max 2)
    // 9. If at least one of the images is uncropped, add entrie's url to the final list
    // 10. Output the final list
}
